{
  "name": "easy-commit-generator",
  "displayName": "Easy Commit Generator",
  "description": "AI 一键生成中英文 Commit Message",
  "version": "0.0.1",
  "publisher": "EasyCommitGenerator",
  "license": "MIT",
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "SCM Providers",
    "Other"
  ],
  "keywords": [
    "git",
    "commit",
    "message",
    "ai",
    "chinese"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "Easy Commit Generator",
      "properties": {
        "easyCommitGenerator.apiBaseUrl": {
          "type": "string",
          "default": "",
          "description": "自定义 OpenAI 兼容 API 的 Base URL（留空则使用 VS Code 内置语言模型）",
          "format": "uri"
        },
        "easyCommitGenerator.apiKey": {
          "type": "string",
          "default": "",
          "description": "自定义 API 的 Key",
          "format": "password"
        },
        "easyCommitGenerator.apiModel": {
          "type": "string",
          "default": "gpt-4o-mini",
          "description": "使用的模型名称（如 gpt-4o, gpt-4o-mini, deepseek-chat 等）"
        }
      }
    },
    "commands": [
      {
        "command": "easyCommitGenerator.generateZhCN",
        "title": "生成中文 Commit Message",
        "icon": "$(comment-discussion-sparkle)"
      },
      {
        "command": "easyCommitGenerator.generateEn",
        "title": "生成英文 Commit Message",
        "icon": "$(comment-discussion-sparkle)"
      }
    ],
    "menus": {
      "scm/title": [
        {
          "command": "easyCommitGenerator.generateZhCN",
          "when": "scmProvider == git",
          "group": "navigation"
        },
        {
          "command": "easyCommitGenerator.generateEn",
          "when": "scmProvider == git",
          "group": "navigation"
        }
      ],
      "scm/sourceControl": [
        {
          "command": "easyCommitGenerator.generateZhCN",
          "when": "scmProvider == git",
          "group": "inline"
        },
        {
          "command": "easyCommitGenerator.generateEn",
          "when": "scmProvider == git",
          "group": "inline"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/vscode": "^1.90.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
